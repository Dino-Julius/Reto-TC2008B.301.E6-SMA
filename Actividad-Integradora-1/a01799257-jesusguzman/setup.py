#!/usr/bin/env python
from setuptools import find_packages, setup

requires = ["mesa"]

setup(
    name="a01799257-jesusguzman",
    version="0.0.1",
    packages=find_packages(),
    install_requires=requires,
)
